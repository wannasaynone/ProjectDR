# 角色互動系統

> **版本**: v2.0

## 系統目的

角色互動系統是村莊經營與戀愛養成的核心介面。玩家透過此系統與角色見面、對話、使用角色對應的專業功能（委託／倉庫）、承接主線任務、送禮。此系統直接支撐 core-definition.md 的核心樂趣 **#2 經營成就感**（委託循環、擴建循環）與 **#3 角色/HCG**（每次互動都是情感累積的機會）。

自 2026-04-17 定案後，角色互動系統涵蓋委託流程的「交給我吧 → 工作中 → 完成回報」循環、主線任務承接、紅點主動發話等機制。

---

## 一、互動流程

### 1.1 基礎流程（正常狀態）

```
Hub 主畫面 VillageHubView
  → 點擊角色按鈕
    → 進入角色互動畫面 CharacterInteractionView
      → 自動播放對話（打字機效果）
        → 對話結束
          → 顯示功能選單（4 項）
            → 點擊功能 → 進入對應功能流程
```

### 1.2 委託中狀態流程（工作中）

角色正在執行委託時（詳見 `commission-system.md`），玩家仍可進入（GC 決策：仍可進入）：

```
玩家進入角色 → 顯示工作中狀態
  立繪保留，對話改為描述角色認真工作的樣子
  選單區變為「工作中 mm:ss」倒數顯示
  玩家僅能退出返回 Hub
```

### 1.3 完成領取狀態流程

紅點亮起時（L1 委託完成 / L4 主線事件）：

```
玩家進入角色 → 播放完成回報對話（取代日常招呼）
  → 顯示產出/事件內容 + [領取/確認] 按鈕
    → 玩家確認 → 產出進入背包 / 事件處理完成
      → 回到第一狀態
```

### 1.4 強制模式狀態流程（節點 0 / 特殊劇情事件）

節點 0 開場時（詳見 `character-unlock-system.md`）與某些主線劇情事件中，interact view 進入**強制模式**：

```
（自動觸發，非玩家主動點擊）
進入角色 interact view → 無返回按鈕（Hub 主動權被鎖）
  → 對話播放（打字機）
    → VN 式選項（必須選擇）
      → 選擇結果在背景生效（按鈕解鎖 / 物資發放 / 劇情推進）
        → 開啟返回按鈕（玩家首次獲得 Hub 主動權）
          → 返回 Hub
```

**特殊規則**：
- 強制模式 = 流程結束前玩家無法逛 Hub、無法進入其他角色
- 返回按鈕在特定時機才開啟（通常是選項完成 + 必要對話結束時）
- 選項必須做出選擇（無「不選」或「取消」選項）
- **選擇後不強制播放劇情演出**（如「帶玩家去見新角色」），角色的登場時刻由玩家主動觸發

**應用場景**：
- 節點 0（開局）：選擇 1 流程
- 節點 1/2 的劇情推進（視劇情設計，可能部分使用強制模式）

### 1.5 首次進入狀態流程（角色登場 CG + 短劇情）

各角色首次被玩家主動點擊時，進入專屬的「首次進入流程」：

```
玩家點擊新解鎖的角色按鈕（首次）
  → 進入 interact view（正常模式，有返回按鈕）
    → 播放角色登場 CG
      → 短劇情（500~1500 字，敘事錨點）
        → 劇情結束 → 進入正常對話狀態
          → 可操作功能選單
```

**特殊規則**：
- 每位角色的「首次進入流程」**只觸發一次**，之後進入為正常流程（1.1）
- 登場 CG + 短劇情不在節點 0 內播放，而是**玩家主動點擊才見到**
- 此流程不是強制模式（玩家可隨時按返回離開）
- 實作需記錄「該角色是否已首次進入」的狀態

---

## 二、角色互動畫面佈局

| 區域 | 位置 | 內容 |
|------|------|------|
| 立繪區 | 左側 | 角色立繪動畫（Animator），IT 階段預留欄位 |
| 對話區 | 右上方 | 對話台詞，帶打字機效果 |
| 功能選單區 | 右下方 | 對話結束後顯示，列出角色對應的 4 個功能按鈕 |
| 工作中狀態區（新） | 右下方（取代功能選單） | 倒數 mm:ss 顯示 + 認真工作對話敘述（工作中狀態時） |
| 提煉介面區（新） | 右側（對話 + 選單消失後） | 格子式工作台（委託交付時），詳見 `commission-system.md` 三章 |

---

## 三、角色功能選單對應（統一四項）

製作人 2026-04-17 第九輪 GA-B + 命名決策：

| 角色 | 功能選單 |
|------|---------|
| 村長夫人 | `[倉庫][任務][對話][送禮]` |
| 農女 | `[耕種][任務][對話][送禮]` |
| 魔女 | `[煉製][任務][對話][送禮]` |
| 守衛 | `[探索周圍][任務][對話][送禮]` |

### 3.1 各按鈕的行為

| 按鈕 | 行為 |
|---|---|
| **[倉庫]** / **[耕種]** / **[煉製]** / **[探索周圍]** | 進入專業功能介面（村長夫人=倉庫 overlay；其他=提煉格子工作台） |
| **[任務]** | 進入主線任務承接流程（詳見 `main-quest-system.md` 3.3） |
| **[對話]** | 玩家主動發問（對應 G5「玩家發問 40 題」） |
| **[送禮]** | 開啟送禮介面（好感度三途徑之一） |

---

## 四、規則

1. **所有角色統一流程**：點擊任何角色都進入相同的互動畫面結構
2. **每次進入都播放對話**：不區分首次/重複，每次進入自動播放對話
   - **例外**：委託工作中狀態下，對話改為「認真工作」描述
   - **例外**：紅點亮時，對話改為完成回報/主動發問/事件敘事（詳見第六章）
3. **送禮完成後返回互動選單**：送禮完成後自動關閉送禮畫面，返回角色互動畫面，播放角色感謝對話，並預留好感度提升特效顯示空間
4. **立繪動畫**：IT 階段預留 Animator 欄位，使用 placeholder 或留空
5. **功能懸浮覆蓋**：點擊 `[倉庫]`、`[送禮]`、`[任務]` 時，功能頁面在角色互動畫面上方懸浮顯示
6. **提煉介面狀態切換**（新）：點擊專業委託按鈕（`[耕種][煉製][探索周圍]`）時，**右側對話框與功能選單消失**，顯示格子式工作台；按「開始」後切回第二狀態並播放「交給我吧」敘事對話，選單變為「工作中 mm:ss」
7. **工作中可進入**（GC 決策）：委託執行中玩家仍可進入該角色 interact view，但無功能按鈕可點，僅能退出
8. **紅點優先序**（新）：紅點亮時對話替換順序為 L1 → L4 → L3 → L2（TBD，具體順序規則於 commission-system.md 四章）

---

## 五、對話發起方向規則（新，2026-04-17 第十一輪決策）

製作人明確區分兩種對話發起方向的內容來源：

| 發起方向 | 機制 | 內容類型 | 對應 G5 規格 |
|---|---|---|---|
| **角色主動發話** | 紅點亮 → 玩家進入 → 自動播放 | **角色問問題** | 角色發問 70 題 |
| **玩家主動發話** | 玩家點擊 `[對話]` → 進入對話模式 | **玩家問問題** | 玩家發問 40 題 |

### 5.1 角色主動發話（紅點觸發）

- 紅點亮起的四層觸發（詳見 `commission-system.md` 四章）：
  - **L1 委託完成層**：完成回報對話
  - **L2 角色發問層**：角色主動問玩家問題
  - **L3 任務層**：任務按鈕紅點（承接主線任務）
  - **L4 事件層**：主線事件觸發
- 進入角色 → 對話**取代**日常招呼 → 玩家被動接收

### 5.2 玩家主動發話（點擊 `[對話]`）

玩家點擊 `[對話]` 後的 UX 流程（TBD，工作室提案）：

```
點擊 [對話]
  → 進入對話模式
    → 顯示可問的問題清單（40 題，依好感度分批解鎖）
      → 玩家選一個問題
        → 角色回答（打字機）
          → 繼續選問題 或 退出對話
```

類似 Visual Novel 的「question tree」結構，具體 UX 待製作人確認。

### 5.3 兩種發起方向的互斥

- 兩者是**不同的對話內容來源**，不重複
- 角色發問的 70 題與玩家發問的 40 題在 G5 規格中是分開的兩類預算

---

## 六、工作中狀態完整設計

製作人 2026-04-17 第八輪 GC 決策：工作中**仍可進入**，選單變倒數顯示、對話改描述角色認真工作。

### 6.1 佈局

```
┌───────────────────────────────────────┐
│                                       │
│   ┌─────────┐      ┌──────────────┐   │
│   │         │      │ 對話：        │   │
│   │  立繪   │      │（認真工作中   │   │
│   │         │      │  的描述）     │   │
│   └─────────┘      ├──────────────┤   │
│                    │ 工作中...    │   │
│                    │    03:11     │   │
│                    └──────────────┘   │
└───────────────────────────────────────┘
```

### 6.2 設計細節

| 元素 | 規格 |
|---|---|
| **立繪** | 使用「認真工作」表情差分（若無則使用「一般」） |
| **對話** | 描述角色正在認真工作的樣子（例：「她專心地混合著藥草，沒有注意到你進來」） |
| **倒數顯示** | `mm:ss` 格式（例：「工作中... 03:11」） |
| **功能選單** | 不顯示四項選單按鈕，以倒數取代 |
| **可執行操作** | 僅能退出 interact view 返回 Hub |
| **情感魅力點** | 玩家看到角色專注工作的樣子——本身就是一種親密感的來源 |

### 6.3 適用對象

- 委託型三位（農女/魔女/守衛）：專業委託執行中
- 村長夫人：倉庫擴建執行中（同樣採「工作中 mm:ss」模式）

---

## 七、打字機效果規格

- 對話台詞逐字顯示
- IT 階段使用固定速度（可配置）
- 點擊/按鍵可跳過打字機效果，直接顯示完整台詞

---

## 八、紅點系統整合

紅點出現的位置與觸發條件：

| 紅點位置 | 觸發層 | 內容類型 |
|---|---|---|
| Hub 畫面角色頭像 | L1（委託完成）、L2（角色發問）、L4（主線事件） | 進入後播放對應對話 |
| Interact view 功能選單的 `[任務]` 按鈕 | L3（新任務可承接） | 點擊後播放任務描述對話 + 跳出承接提示視窗 |

詳細紅點機制見 `commission-system.md` 第四章「紅點觸發四層」。

---

## 九、版本更新紀錄

| 版本 | 日期 | 說明 |
|------|------|------|
| v1.0 | 2026-04-14 | 初版，根據製作人口述規格整理 |
| v1.1 | 2026-04-15 | 新增「送禮」功能至所有角色的功能選單對應表 |
| v1.2 | 2026-04-15 | 新增規則 6：送禮完成後返回互動選單、播放感謝對話、預留好感度特效空間 |
| v2.0 | 2026-04-17 | 委託制定案整合：功能選單擴為統一四項（專業/任務/對話/送禮）；新增提煉介面狀態、工作中狀態、完成領取狀態；新增對話發起方向規則（角色主動=角色發問 70 題、玩家主動=玩家發問 40 題）；新增紅點系統整合（四層觸發）；按鈕命名更新（耕種/煉製/探索周圍）；交叉引用 `commission-system.md`、`main-quest-system.md` |
| v2.1 | 2026-04-17 | 新增 1.4 強制模式狀態流程（節點 0 開場使用，返回按鈕被鎖直到選擇 1 完成 + 劇情演出結束）；對應 `character-unlock-system.md` 節點 0 的 UX 澄清 |
| v2.2 | 2026-04-17 | 節點 0 UX 進一步澄清（第十九輪修正）：**選擇後不強制播放劇情演出**（如「帶去見」）；新增 1.5 首次進入狀態流程（角色登場 CG + 短劇情由玩家主動點擊觸發，不在節點 0 播放） |
