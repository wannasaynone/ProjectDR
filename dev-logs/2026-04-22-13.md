# Dev Log 2026-04-22-13 — A6-1/A6-2 GoogleSheet2JsonSetting 串接 + JSON→TXT 改名

**Sprint**: Sprint 7 (sprint-7-it-to-vs-restructure.md)
**工作項目**: A6 GoogleSheet2JsonSetting 匯出工具串接（技術實作批次）
**執行者**: dev-agent
**遵循 ADR**: ADR-002 [C01]（IT 階段例外退出 Gate）

---

## 完成項目

### A6-1：Setting 資產搬家 + 填值

**搬移確認**：
- 舊路徑：`Assets/Resources/Data/_Google Sheet 2 Json Setting.asset`（已為空目錄，Setting 早已不在此）
- 新路徑：`Assets/Game/Resources/Config/_Google Sheet 2 Json Setting.asset`（確認已存在）
- 空目錄 `Assets/Resources/Data/` 刪除完成

**填值確認**（透過 `manage_scriptable_object` 修改）：
- `sheetID`: `1WjvMHTtEZ-JIRE1hmKz0pQMfI78hVI9jXC7ZLKEpLoo`
- `sheetNames`（13 個）：
  1. AffinityConfig
  2. CGSceneConfig
  3. CharacterIntroConfig
  4. CharacterQuestionsConfig
  5. CombatConfig
  6. CommissionRecipes
  7. GreetingConfig
  8. IdleChatConfig
  9. MainQuestConfig
  10. MonsterConfig
  11. NodeDialogueConfig
  12. StorageExpansionConfig
  13. InitialResourcesConfig

注意：Google Sheets MCP 工具（`mcp__google-sheets__list_sheets`）在本 session 不可用，分頁名稱依 ADR-002 [A] 區塊 13 個需匯出的 tabular config 填入。若製作人建立的分頁名稱與上方不一致，需於觸發 Convert 前先核對。

### A6-2：15 個 JSON 改名為 TXT

以 bash mv 指令批量改名（Unity MCP manage_asset 對 .json 操作有限制），同時 mv 對應 .meta 檔，GUID 完整保留。

改名清單（15 個）：
| 原檔名 | 新檔名 |
|--------|--------|
| `affinity-config.json` | `affinity-config.txt` |
| `cg-scene-config.json` | `cg-scene-config.txt` |
| `character-intro-config.json` | `character-intro-config.txt` |
| `character-questions-config.json` | `character-questions-config.txt` |
| `combat-config.json` | `combat-config.txt` |
| `commission-recipes-config.json` | `commission-recipes-config.txt` |
| `gift-sword-config.json` | `gift-sword-config.txt` |
| `greeting-config.json` | `greeting-config.txt` |
| `idle-chat-config.json` | `idle-chat-config.txt` |
| `initial-resources-config.json` | `initial-resources-config.txt` |
| `main-quest-config.json` | `main-quest-config.txt` |
| `monster-config.json` | `monster-config.txt` |
| `node-dialogue-config.json` | `node-dialogue-config.txt` |
| `player-questions-config.json` | `player-questions-config.txt` |
| `storage-expansion-config.json` | `storage-expansion-config.txt` |

**GUID 保留驗證**：
- 以 Scene 引用的 `affinity-config`（GUID: `f21734a2aa6e8b44093085803457b466`）為例，.meta 確認 GUID 完整保留
- `VillageEntryPoint` 的 `[SerializeField] TextAsset` 欄位皆走 GUID 引用，rename 後 Unity refresh 無新 error
- 另：`ExplorationEntryPoint` 用 `Resources.Load<TextAsset>("Config/combat-config")` 動態載入（不含副檔名），改 .txt 後仍可正常讀取

**注意事項（affinity-config 路徑修復）**：
- Unity MCP `rename` 嘗試時誤將 `affinity-config.txt` 放至 `Assets/` 根目錄
- 發現後用 bash 移回 `Assets/Game/Resources/Config/affinity-config.txt`（連同 .meta）
- Unity refresh 後確認無 error

### A6-3：Convert 觸發方式

- KGC 的「Start Convert」按鈕為 Custom Inspector（`GoogleSheet2JsonSettingEditor.cs`）按鈕，不是 menu item
- Unity MCP 無法從 menu_items 觸發此按鈕
- **需製作人手動操作**：在 Unity Editor → 選取 `Assets/Game/Resources/Config/_Google Sheet 2 Json Setting.asset` → Inspector 中點「Start Convert」
- Convert 完成後將覆寫/建立 13 個 `{SheetName}.txt`（注意：輸出檔名為 sheetNames 中的名稱，如 `AffinityConfig.txt`，與現有的 `affinity-config.txt` 是不同檔案）

---

## 整合衝突審查結果（Phase 2.5）

- **TextAsset 副檔名相容性**：Unity TextAsset 對 .txt 和 .json 均使用 `TextScriptImporter`，importer 類型不因改名而改變，相容性確認
- **Inspector 引用**：所有 [SerializeField] TextAsset 欄位走 GUID，.meta 保留後無引用斷裂
- **Resources.Load**：不含副檔名，.txt 和 .json 均可找到
- **Console Error**：Unity refresh 後無新 config 相關 error

---

## 狀態檔更新

- Sprint 7 A5 checkbox: [x]（製作人已完成 A5）
- Sprint 7 A6 checkbox: [x]（技術實作完成；文件部分待 dev-head）
- ADR-002 [C01] checkbox: [x]
- project-status.md 決策記錄: 已 append
- FILE_MAP.md: 15 個 .json → .txt 更新，新增 Setting.asset 條目，刪除 Assets/Resources/Data/ 目錄條目

---

## 待製作人操作

1. 在 Unity Editor 選取 `Assets/Game/Resources/Config/_Google Sheet 2 Json Setting.asset`
2. 在 Inspector 確認 sheetID 與 sheetNames（13 個）已填入
3. 點擊「Start Convert」按鈕，等待 Console 顯示「---End---」
4. 確認 13 個 `{SheetName}.txt` 在 `Assets/Game/Resources/Config/` 下被建立/更新
5. 確認 Console 無 HTTP 401/403 error（若有 → API Key 問題，回報 dev-head）

## Blocker 狀態

- 無阻擋項（Google Sheets MCP 不可用不影響工具串接本身）
- 測試未能執行（Unity session 在 refresh 後持續 unavailable，推測 domain reload 尚未完成）
