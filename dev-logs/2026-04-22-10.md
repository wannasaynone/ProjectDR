# Dev Log 2026-04-22-10

**任務**: Sprint 7 A2 — A08 GuardFirstMeetDialogueConfig 併入 NodeDialogueConfig（製作人 2026-04-22 拍板）
**遵循 ADR**: ADR-002（Manifest Version: 2026-04-22）
**測試結果**: 1426 / 1426 全綠（0 失敗）

---

## 變更摘要

### 背景

Sprint 7 E6 階段 ADR-002 A08 條目原標記為豁免候選（`EXEMPT: ADR-002 A08`），理由是 `GuardFirstMeetDialogueConfigData` 確認仍在使用（VillageEntryPoint 消費點），非 dead code。

製作人 2026-04-22 拍板：不走豁免路徑，改為**重構併入 NodeDialogueConfig**，本質屬節點式對話資料擴充，比豁免更徹底的解法。

---

## 具體動作

### 1. node-dialogue-config.json — 加入 4 筆守衛首次對白

新增 `node_id="guard_first_meet"` 節點（id 32~35）：

| id | line_id | speaker | line_type | sequence |
|----|---------|---------|-----------|----------|
| 32 | guard_first_meet_001 | guard | dialogue | 1 |
| 33 | guard_first_meet_002 | guard | dialogue | 2 |
| 34 | guard_first_meet_003 | narrator | narration | 3 |
| 35 | guard_first_meet_004 | guard | dialogue | 4 |

note 欄位補充：`node_id=guard_first_meet 為守衛首次進入互動畫面自動觸發的取劍對白（原 A08 併入，2026-04-22）`

### 2. NodeDialogueController.cs — 擴充首次觸發 API

新增：
- `public const string NodeIdGuardFirstMeet = "guard_first_meet"`
- `private readonly HashSet<string> _playedOnceNodes` — session 內記憶體旗標（禁用 PlayerPrefs）
- `public bool TryPlayFirstMeetDialogueIfNotTriggered(string nodeId)` — 若未觸發則播放並標記已觸發；已觸發、IsPlaying 中、DialogueManager 使用中、節點不存在時靜默回傳 false

fallback：節點不存在時記錄 Warning 但不拋例外，不阻斷流程。

### 3. VillageEntryPoint.cs — 消費點重構

**前**：
```csharp
if (characterId == CharacterIds.Guard && !_guardFirstMeetDialogueCompleted
    && _characterUnlockManager != null && _characterUnlockManager.IsUnlocked(CharacterIds.Guard)
    && view != null)
{
    string[] lines = System.Linq.Enumerable.ToArray(_guardFirstMeetDialogueConfig.DialogueLines);
    view.SetFirstMeetOverrideDialogue(lines, OnGuardFirstMeetDialogueCompleted);
}
```

**後**：
```csharp
if (characterId == CharacterIds.Guard
    && _characterUnlockManager != null && _characterUnlockManager.IsUnlocked(CharacterIds.Guard)
    && _nodeDialogueController != null)
{
    _nodeDialogueController.TryPlayFirstMeetDialogueIfNotTriggered(NodeDialogueController.NodeIdGuardFirstMeet);
}
```

移除 `OnGuardFirstMeetDialogueCompleted` 方法，業務邏輯移入 `OnNodeDialogueCompletedForMainQuest`：

```csharp
if (e.NodeId == NodeDialogueController.NodeIdGuardFirstMeet)
{
    // 發劍 + 探索重開
    var grants = _initialResourcesConfig.GetGrantsByTrigger(InitialResourcesTriggerIds.GuardSwordAsked);
    foreach (InitialResourceGrant grant in grants)
        _initialResourceDispatcher.Dispatch(grant);
    EventBus.Publish(new ExplorationGateReopenedEvent());
}
```

移除欄位：`_guardFirstMeetDialogueConfig`（`GuardFirstMeetDialogueConfig`）、`_guardFirstMeetDialogueCompleted`（`bool`）、`[Header("Guard First Meet Dialogue")] [SerializeField] TextAsset _guardFirstMeetDialogueConfigJson`

### 4. VillageEntryPointInstallers.cs — 移除 A08 載入邏輯

刪除 `GuardFirstMeetDialogueConfigData` JSON 解析與 `_guardFirstMeetDialogueConfig` 建構段落。

### 5. 刪除 A08 獨立檔案（透過 MCP manage_asset）

- `Assets/Game/Scripts/Village/Guard/Data/GuardFirstMeetDialogueConfigData.cs`（+ .meta）
- `Assets/Game/Resources/Config/guard-first-meet-dialogue-config.json`（+ .meta）
- `Assets/Game/Scripts/Village/Guard/Data/`（空目錄，+ .meta）

Guard/Manager 目錄（含 GuardReturnEventController）保留不動。

### 6. 整合測試重構 — GuardFirstMeetDialogueIntegrationTest.cs

原測試依賴 `GuardFirstMeetDialogueConfig` 類別，重構後改為依賴 `NodeDialogueController`：

- T1：`TryPlayFirstMeetDialogueIfNotTriggered` 首次呼叫 → 回傳 true、IsPlaying = true
- T2：`SimulateGuardFirstMeetCompleted`（模擬 VEP 業務邏輯）→ 劍入背包 + ExplorationGateReopenedEvent
- T3：`TryPlayFirstMeetDialogueIfNotTriggered` 再次呼叫（已觸發）→ 回傳 false
- T4：T2 production path — ExplorationGateReopenedEvent → T2 主線完成
- T5/T5b：regression 測試（guard_ask_sword 不在清單 / FirstMeet 清除時機）— 原樣保留

---

## Phase 2.5 整合衝突審查結論

| 項目 | 狀況 |
|------|------|
| 事件時序 | NodeDialogueController 播放完成後發布 NodeDialogueCompletedEvent → VEP 訂閱者發劍 + ExplorationGateReopenedEvent；時序正確（對白先，業務邏輯後） |
| 方法副作用 | TryPlayFirstMeetDialogueIfNotTriggered 僅添加 HashSet 旗標 + 呼叫 PlayNode；無額外副作用 |
| 動畫衝突 | PlayNode 在 IsPlaying 或 DialogueManager.IsActive 時靜默跳過，不衝突 |
| Fallback | 若節點不存在（config 載入失敗），TryPlay 靜默回傳 false + LogWarning；守衛進入後無對白但不崩潰 |
| 發劍時機 | GuardReturnEventController（GuardReturnEventCompletedEvent）的發劍在 MarkIntroCGPlayed 路徑，與 guard_first_meet 路徑不同；不衝突 |

---

## 文件更新

- `projects/ProjectDR/Assets/Game/Resources/Config/node-dialogue-config.json` — 加 4 筆 + note 更新
- `projects/ProjectDR/Assets/Game/Scripts/Village/Dialogue/Manager/NodeDialogueController.cs` — 新增常數 + HashSet + TryPlay API
- `projects/ProjectDR/Assets/Game/Scripts/Village/Core/Manager/VillageEntryPoint.cs` — 消費點重構、移除欄位/方法
- `projects/ProjectDR/Assets/Game/Scripts/Village/Core/Manager/VillageEntryPointInstallers.cs` — 移除 A08 載入
- `projects/ProjectDR/Assets/Tests/Editor/Village/Integration/GuardFirstMeetDialogueIntegrationTest.cs` — 重構測試
- `projects/ProjectDR/adrs/ADR-002-it-stage-exemption-exit.md` — A08 條目更新為 ✅ 已併入；v1.3 Status History
- `projects/ProjectDR/sprint/sprint-7-it-to-vs-restructure.md` — A2 checkbox 描述更新
- `projects/ProjectDR/FILE_MAP.md` — 移除 A08 條目，更新整合測試描述
- `projects/ProjectDR/project-status.md` — 加 A08 併入決策記錄
- `projects/ProjectDR/session-state/active.md` — 進度更新
