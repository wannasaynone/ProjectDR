# Dev Log 2026-04-22-1

> **Sprint**: Sprint 7 — VS 前徹底重構
> **批次**: E1 基礎設施（步驟 1/2/5/6） + C1+C2 工具層守護
> **執行者**: dev-agent
> **Review Mode**: full

## 完成工作

### E1 — 步驟 1：21 模組資料夾骨架（ADR-004）

建立以下 Village 模組資料夾（各含 Manager/View/Data/Presenter/Interface 五層 + .keep）：
- `Village/Affinity/`、`Village/Alchemy/`、`Village/Backpack/`、`Village/CG/`
- `Village/CharacterInteraction/`、`Village/CharacterIntro/`、`Village/CharacterQuestions/`
- `Village/CharacterStamina/`、`Village/CharacterUnlock/`、`Village/Commission/`
- `Village/Dialogue/`、`Village/Farm/`、`Village/Gift/`、`Village/Greeting/`
- `Village/GuardReturn/`、`Village/IdleChat/`、`Village/ItemType/`
- `Village/MainQuest/`、`Village/NodeDialogue/`、`Village/OpeningSequence/`
- `Village/Progression/`、`Village/Storage/`、`Village/TimeProvider/`
- `Village/Navigation/`、`Village/Shared/`、`Village/Core/`（含 5 層）

### E1 — 步驟 2：B2/B3 Village Core 契約層（ADR-003）

新建 8 個 Core 契約檔：

**介面層** (`Village/Core/Interface/`)：
- `IVillageInstaller.cs` — Install(ctx)/Uninstall() 對稱契約（ADR-003 D1）
- `IVillageTickable.cs` — 選擇性 Tick 介面（非強制繼承）
- `IAffinityQuery.cs` — 好感度唯讀查詢（GetLevel/IsThresholdReached）
- `IVillageProgressionQuery.cs` — 進度唯讀查詢（IsCharacterUnlocked/IsExplorationUnlocked）
- `IStorageQuery.cs` — 倉庫唯讀查詢（GetItemCount/HasItem）
- `IBackpackQuery.cs` — 背包唯讀查詢（GetItemCount/HasItem/IsFull）

**資料層** (`Village/Core/Data/`)：
- `GameDataQuery.cs` — `delegate T GameDataQuery<T>(int id) where T : class, IGameData`（隔離靜態依賴）
- `VillageContext.cs` — 9 欄跨 Installer 共用容器（≤10 硬指標）；建構器注入 Canvas/UIContainer/GameDataAccess；其餘 internal set

**VillageContext 過渡狀態**：暫時引用 `ProjectDR.Village.UI.ViewStackController` + `ProjectDR.Village.ITimeProvider`（搬移步驟 3 完成後需更新 namespace）

### E1 — 步驟 5（A01）：AffinityCharacterConfigData 改造

- `AffinityConfigData.cs`：`AffinityCharacterConfigData` 加 `IGameData`，新增 `public int id; public int ID => id;`，保留 `characterId`（語意字串外鍵，雙欄位規則）
- `affinity-config.json`：每筆 character 加 `"id"` 欄位（VillageChiefWife=1, Guard=2, Witch=3, FarmGirl=4）
- `AffinityManagerTests.cs`：新增 4 個 ADR-001 契約驗證測試

### E1 — 步驟 6（A05）：CombatConfigJson 改造

- `CombatConfigData.cs`：`CombatConfigJson` 加 `IGameData`，新增 `public int id; public int ID => id;`（singleton 型，ID=1）
- `combat-config.json`：頂層加 `"id": 1`
- `CombatConfigDataTests.cs`：新建 6 個測試（IGameData 契約驗證 + 反序列化正確性）

### C1+C2：validate-file-size.sh hook

- `validate-file-size.sh`：檔案 > 800 行警示 / > 1200 行強警示 / 單方法 > 100 行警示（awk 靜態分析 C# 方法邊界）
- `settings.json`：PostToolUse Write|Edit 已加入 `validate-file-size.sh`（timeout 5s）

## BLOCKER

**步驟 3：搬移 Shared/View、Navigation、TimeProvider、ItemType、Core 檔案**

- 需要透過 Unity Editor 批次移動以保留 meta GUID
- Unity MCP 未在 settings.json 設定，無法使用 `manage_asset` 工具
- 根據任務 Blocker 原則：「Unity MCP 不支援批次搬檔保留 GUID → 停下回報，讓工作室管理層決定怎麼處理」

**步驟 4（namespace 遷移）** 與 **步驟 7（smoke test）** 均等待步驟 3 完成後執行。

**VillageContext 過渡 namespace** 已記錄，等步驟 3 後更新。

## 遵循的 ADR

- ADR-001（IGameData 契約）
- ADR-002 A01/A05（IT 階段例外退出）
- ADR-003（VillageContext 容器設計）
- ADR-004（21 模組 × 5 層骨架）
- Manifest Version: 尚未建立（C3 未執行，ADR-003 Accepted 後觸發）
