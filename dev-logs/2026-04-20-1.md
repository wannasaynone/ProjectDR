# Dev Log — 2026-04-20-1

> **日期**: 2026-04-20
> **Sprint**: Sprint 6 — 探索開放流程重構（B 類 Config 修改）
> **涉及範圍**: B1 / B2 / B3（Config JSON）

---

## 工作摘要

Sprint 6 B 類三項 Config 修改，依 2026-04-20 製作人 6 個決策執行：

---

## B1 — main-quest-config.json

**路徑**: `Assets/Game/Resources/Config/main-quest-config.json`

**改動內容**:
- **刪除** `quest_id = "T2"` 條目（「幫她一次」，commission_count 完成條件）
- **刪除** `quest_id = "T3"` 條目（「再去認識另一個人」，commission_count 完成條件）
- **重寫** `quest_id = "T1"` 條目：
  - `display_name`: 「先去認識她們」→「認識所有人」
  - `description`: placeholder 更新為「村長夫人說，還有幾個人你沒見過。去認識她們吧。」
  - `completion_condition_type`: `dialogue_end`（不變，但語意改為節點 2 對話完成）
  - `completion_condition_value`: `first_char_intro_complete` → `node_2_dialogue_complete`
  - `reward_grant_ids`: `unlock_farm_girl_seed|unlock_witch_herb` → `""`（空字串，決策 6 不發初始物資）
  - `unlock_on_complete`: `T2|choice1_complete` → `T2|node_2_complete|exploration_open`
- **重寫** `quest_id = "T4"` → 改為 `quest_id = "T2"`：
  - sort_order: 4 → 2
  - 其餘欄位（display_name / description / completion_condition / reward_grant_ids / unlock_on_complete）保持原 T4 內容
- `note` 欄位更新，加入 Sprint 6 重構說明

**重構後結構**:
```
T0「醒來的地方」   sort_order=0  auto / node0_dialogue_complete
T1「認識所有人」   sort_order=1  dialogue_end / node_2_dialogue_complete（reward=空）
T2「出去看看外面」 sort_order=2  first_explore / guard_return_event_complete（reward=unlock_guard_sword）
```

---

## B2 — initial-resources-config.json

**路徑**: `Assets/Game/Resources/Config/initial-resources-config.json`

**改動內容**:
- **刪除** `grant_id = "unlock_farm_girl_seed"` 整條 grant（農女解鎖番茄種子 ×3）
- **刪除** `grant_id = "unlock_witch_herb"` 整條 grant（魔女解鎖綠藥草 ×3）
- **保留** `initial_backpack_node0`（節點 0 空背包，無物資）
- **保留** `unlock_guard_sword`（守衛歸來事件贈劍，T2 完成條件）

**引用檢查**（搜尋結果）:
- `Assets/Game/Scripts` 中無 `unlock_farm_girl_seed` / `unlock_witch_herb` 的直接硬編引用
- 程式層 `CharacterUnlockManager` 使用 trigger_id 查詢，與 grant_id 解耦；C 類工作需清理 `InitialResourceDispatcher` 相關邏輯
- `CharacterUnlockManagerTests.cs` 與 Integration tests 內有舊 grant_id 引用 → 列入 C/D 類 regression 清單

---

## B3 — node-dialogue-config.json

**路徑**: `Assets/Game/Resources/Config/node-dialogue-config.json`

**結論**：無需修改。

原因：
1. config 結構僅含對話內容欄位（line_id / node_id / sequence / speaker / text / line_type / choice_branch），無 `trigger_condition` 欄位
2. 節點 2 觸發邏輯（「魔女對話結束瞬間觸發 L4 紅點」）由程式層決定，屬 C 類（MainQuestManager / RedDotManager）
3. 全文搜尋確認無 T3 / T4 / commission 等舊字串殘留

---

## TDD 測試更新

### MainQuestConfigTests.cs 更新

新增/修改測試案例：
- `GetQuest_ReturnsExpectedInfo`：T1 completion_condition_value 改為 `node_2_dialogue_complete`
- `T1_RewardGrantIds_IsEmpty`（新增）：T1 reward_grant_ids 應為空
- `T1_UnlockOnComplete_ContainsT2AndNode2AndExploration`（新增）：T1 解鎖 T2 / node_2_complete / exploration_open
- `T2_SortOrder_IsTwo`（新增）：原 T4 重編後 sort_order = 2
- `OldT2T3_DoNotExist`（新增）：config 中無 CommissionCount 型別任務
- `RealJsonFile_DeserializesSuccessfully`：改為驗 3 條任務（count=3），T3/T4 應為 null
- `BuildSimpleConfig`：重寫為新 T0/T1/T2 結構

### InitialResourcesConfigTests.cs 更新

新增測試案例：
- `Sprint6_FarmGirlSeedGrant_DoesNotExist`（新增）：unlock_farm_girl_seed 應為 null
- `Sprint6_WitchHerbGrant_DoesNotExist`（新增）：unlock_witch_herb 應為 null
- `Sprint6_GuardSwordGrant_StillExists`（新增）：unlock_guard_sword 保留且值正確
- `Sprint6_InitialBackpackNode0_StillExists`（新增）：initial_backpack_node0 保留
- `RealJsonFile_DeserializesSuccessfully`：改為驗農女/魔女 grant 為 null，守衛/背包保留
- `BuildConfig`：移除農女/魔女 grants，僅保留背包 + 守衛劍

---

## Regression 清單（C 類需處理）

以下測試因本次 config 改動預計失敗，屬於 C/D 類工作範疇：

| 測試檔 | 失敗原因 |
|--------|---------|
| `MainQuestManagerTests.cs` | 內部 BuildConfig 含 T2/T3/T4 四條，流程驗測引用舊 T 編號；`first_char_intro_complete` 信號值 |
| `CharacterUnlockManagerTests.cs` | L95 驗 `unlock_farm_girl_seed` grant；L221 `T3_UnlocksExplorationFeature` 訂閱舊 T3 事件；BuildConfig 含舊 grants |
| `NodeProgressionIntegrationTest.cs` | T2/T3 狀態推進邏輯、BuildConfig 含舊 grants、四條序列驗測 |
| `OpeningFlowIntegrationTest.cs` | BuildConfig 含舊 grants |

B 類工作（Config JSON + 對應測試檔 TDD 更新）無 regression。
以上失敗均來自舊程式邏輯綁定舊 T 編號，是 C/D 類必須處理的預期失敗。

---

## 遵循的 ADR

無對應 ADR（本次為純 config data 修改，不含架構決策）。
遵循工作室規則：禁止硬編數值（config 外部化）、禁止 git 操作。
